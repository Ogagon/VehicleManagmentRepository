@model MonoTask.MVC.ViewModels.VehicleMakeViewModel

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<div>
    <h4>Vehicle make details page</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>@Html.DisplayNameFor(model => model.Name)</dt>
        <dd>@Html.DisplayFor(model => model.Name)</dd>

        <dt>@Html.DisplayNameFor(model => model.Abrv)</dt>
        <dd>@Html.DisplayFor(model => model.Abrv)</dd>

        @if (Model.Models != null && Model.Models.Any())
        {
            <dt>Models</dt>
            <dd>
                    @foreach (var item in Model.Models)
                    {
                        @Html.DisplayFor(m => item.Display)<br />
                    }
            </dd>
        }
    </dl>
</div>

<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.Id }) |
    @Html.ActionLink("Back to List", "Index")
</p>
